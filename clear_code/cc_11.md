# 1. 
Добавила в vs code предупреждение о неиспользуемых переменных.

# 2., 3
// Изначальный код:
```
def odometer(oksana: list) -> int:
    total = 0
    for i in range(len(oksana)):
        hours = 0
        if i == 0:
            hours = oksana[i+1]
            total += oksana[i] * hours
        elif i % 2 == 0:
            hours = oksana[i+1] - oksana[i-1]
            total += oksana[i] * hours

    return total
```
// Убрала ненужную инициализацию переменной hours. Добавила инвариант.
```
def odometer(oksana: list) -> int:
    total = 0
    for i in range(len(oksana)):
        if i == 0:
            hours = oksana[i+1]
            total += oksana[i] * hours
        elif i % 2 == 0:
            hours = oksana[i+1] - oksana[i-1]
            total += oksana[i] * hours
    assert total > -1
    return total
```
# 4.
// Изначальный код:
```
import os
def ReturnNameList(dir_path, file_extention, b_flag):
    files_list = []
    dirs_list = []
    final_list = []
    # цикл и дальнейшая обработка
    final_list.append(files_list)
    final_list.append(dirs_list)
    assert final_list is not None
    return final_list
```
// Обнулила значения переменных, содержащих файлы и директории. Поправила названия переменных и функции, обозначила типы параметров.
```
import os
def return_name_list(directory_path: str, file_extention: str, list_quality: bool) -> list:
    files_list = []
    dirs_list = []
    final_list = []
    # цикл и дальнейшая обработка
    final_list.append(files_list)
    final_list.append(dirs_list)
    files_list = []
    dirs_list = []
    assert final_list is not None
    return final_list
```
# 5.
// Изначальный код:
```
def DeleteDir(path):
    search = ReturnNameList(path, ".*", False)
    if len(search[1]) > 0:
            return False
    for file in search[0]:
        os.remove(os.path.join(path,file))
    os.rmdir(path)
    return True
```
// Обнулила значения переменной с результатами поиска. Поправила названия переменной и функции, обозначила типы параметров.
```
def delete_directory(path: str) -> bool:
    search_result = ReturnNameList(path, ".*", False)
    if len(search_result[1]) > 0:
            return False
    for file in search_result[0]:
        os.remove(os.path.join(path,file))
    os.rmdir(path)
    search_result = None
    return True
```
# 6.
// Изначальный код:
```
def hash_fun(self, value):
    hash = sum(value.encode('utf-8'))
    index = hash % self.size
    return index
```
// Добавила инвариант. Поправила название метода.
```
def hash_funсtion(self, value):
    hash = sum(value.encode('utf-8'))
    index = hash % self.size
    assert index > -1
    return index
```
# 7.
// Изначальный код:
```
def to_index(self, key):
    return int(key, 2)
```
// Добавила инвариант.
```
def to_index(self, key):
    assert index > -1
    return int(key, 2)
```
# 8.
// Изначальный код:
```
def squirrel(N: int) -> int:
    factorial = 1
    for i in range(2, N+1):
        factorial = factorial * i
    
    first_digit = factorial
    while first_digit > 10:
        first_digit //= 10
    return first_digit
```
// Добавила инвариант. 
```
def squirrel(N: int) -> int:
    factorial = 1
    for i in range(2, N+1):
        factorial = factorial * i
    
    first_digit = factorial
    while first_digit > 10:
        first_digit //= 10

    assert first_d > 0
    assert first_d < 10
    return first_digit
```
# 9.
// Изначальный код:
```
def PrintingCosts(Line: str) -> int:

    # дан словарь со стоимостью печати элемента.

    summ = 0
    for i in Line:
        if i not in instruction:
            summ += 23
        summ += instruction.get(i)

    return summ
```
// Добавила инвариант. 
```
def PrintingCosts(Line: str) -> int:

    # дан словарь со стоимостью печати элемента.

    summ = 0
    for i in Line:
        if i not in instruction:
            summ += 23
        summ += instruction.get(i)
    
    assert summ > -1
    return summ
```
# 10.
// Изначальный код:
```
def MaximumDiscount(N: int, price: list) -> int:

    price.sort(reverse=True)
    bulk = []
    summ = 0
    for i in range(N):
        bulk.append(price[i])
        if len(bulk) % 3 == 0:
            summ += min(bulk)
            bulk = []

    return summ
```
// Добавила инвариант. 
```
def MaximumDiscount(N: int, price: list) -> int:

    price.sort(reverse=True)
    bulk = []
    summ = 0
    for i in range(N):
        bulk.append(price[i])
        if len(bulk) % 3 == 0:
            summ += min(bulk)
            bulk = []
    assert summ > -1
    return summ
```
# 11.
// Изначальный код:
```
def second_max(list1: list) -> float:
    if len(list1) <= 1:
        return None
    max1 = max(list1)
    if list1.index(max(list1)) == 0:
        max2 = list1[1]
    else:
        max2 = list1[0]
    return second_max_rec(list1, 0, max1, max2)
```
// Добавила инвариант. 
```
def second_max(list1: list) -> float:
    if len(list1) <= 1:
        return None
    max1 = max(list1)
    if list1.index(max(list1)) == 0:
        max2 = list1[1]
    else:
        max2 = list1[0]
    assert max2 in list1
    return second_max_rec(list1, 0, max1, max2)
```
# 12.
// Изначальный код:
```
def sort_mass(x):
    ind = 0
    
    while ind < len(x):
     # тело цикла
        ind += 1
    return x
```
// Объявила переменную-счетчик в заголовке цикла и добавила определение типов.
def sort_mass(x: list) -> list:
    for i in range(len(x)):
    # тело цикла

    return x
```
# 13.
// Изначальный код:
```
def MisterRobot(N: int, data: list) -> bool:

    subset = data.copy()

    i = 0
    while i < N-2:
       # тело цикла
        i += 1

    return subset[-2] < subset[-1]
```
// Объявила переменную-счетчик в заголовке цикла.
```
def MisterRobot(N: int, data: list) -> bool:

    subset = data.copy()

    for i in range(N-2):
       # тело цикла

    return subset[-2] < subset[-1]
```
# 14.
// Изначальный код, метод повышения уровня класса Persona:
```
def LevelUp(self, levels):
    self.current_level += levels
```
// Добавила инвариант класса:
```
def LevelUp(self, levels):
    self.current_level += levels
    assert self.current_level >= self.min_level
```
# 15.
// Изначальный код:
```
def resize(self, new_capacity):
    if self.bank < self.count:
        raise RuntimeError('Not enough tokens')
    new_array = self.make_array(new_capacity)
    for i in range(self.count):
        new_array[i] = self.array[i]
    self.bank -= self.count
    self.array = new_array
    self.capacity = new_capacity
```
// Добавила инвариант класса:
```
def resize(self, new_capacity):
    if self.bank < self.count:
        raise RuntimeError('Not enough tokens')
    new_array = self.make_array(new_capacity)
    for i in range(self.count):
        new_array[i] = self.array[i]
    self.bank -= self.count
    self.array = new_array
    self.capacity = new_capacity
    assert self.capacity > -1
```
