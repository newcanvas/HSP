1.  s1_list = S1.split(' '), s2_list = S2.split(' ') - MAP_COORDINATES = S1.split(' '), TANKS_COORDINATES = S2.split(' ')
// Во-первых, добавила значимые имена, отражающие содежание переменных - карта и группировка танков. Во-вторых, обозначила их как константы, чтобы они не подразумевали изменений значений в ходе программы.

// Дальше придумываю удачные применения констант к контексте кода, так как в своем коде примеров не нашла.

2. Класс BrickHouse, имеет атрибут WALL_MATERIAL = 'Brick', обозначающий материал всех объектов этого класса.

3. В программе по рассчету налогов уместно использовать константы с размером налоговой ставки: TAX_NDS_STANDARD = 22, TAX_NDS_BENEFIT = 10.

4. Для стрима в хадуп по одному и тому же адресу можно использовать константу HDFS_PATH содержащую этот адрес.

5. Для программы анализирующей показатели ряда продуктов, список этих продуктов можно обозначить константой PRODUCT_IDS = (...).

6. В класск Employee локальной программы у всех сотрудников будет одинаковое место работы, так что COMPANY_NAME логично сделать константой.

7. Для использования в рассчетах физических уравнений можно использовать константы по типу G_FORCE, WATER_BOILING_POINT, SPEED_OF_LIGHT.

8. При работе со скриптами, требующими авторизации, можно завести свое имя как константу USER_NAME.

9. Для классов трейдинговых сделок FlexDeal, ForexDeal константа DEAL_TYPE может содержать тип сделки. 

10. Внутри скрипта с расписанием дага название контейнера неизменно, так что CONTAINER_NAME лучше сделать константой.

11. В скрипте с рассылкой можно завети константу со списком почт адресатов EMAIL_LIST = [].

12. В курсе по алгоритмам обсуждался прием, где хвост и голова связного списка занимаются подложным узлом для того, чтобы при добавлении новых узлов значения начала и конца не приходилось обновлять. Для этого при создании нового списка их зачения можно обозначить константами HEAD_VALUE и TAIL_VALUE.
